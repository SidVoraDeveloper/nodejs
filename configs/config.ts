// export default { JWT_SECRET: process.env.JWT_SECRET };
export default { JWT_SECRET: 'test' };

/* if (!process.env.JWT_SECRET) {
  console.log('JWT_SECRET not set.');
  process.exit(1);
} */
